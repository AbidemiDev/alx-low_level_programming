#include <stdio.h>
#include "main.h"
/**
 * main - function that takes a pointer to an int as parameter
 * Return: 0 on sucess
 */
void reset_to_98(int *n)
{
	int np;
	*n = np;

	np = 98;
	return (0);
}
